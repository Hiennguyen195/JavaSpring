spring:
  application:
    name: my-application

  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/my_database
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
      hibernate:
          ddl-auto: update
      show-sql: true
